version: '3.8'

services:
  log-generator:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=dev
      - SERVER_PORT=8000
      - AWS_REGION=us-east-1
      - LOG_GROUP_NAME=/kiro/microservices/backend
